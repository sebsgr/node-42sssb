export { };

let x: number = 3;

function bar(n: number, s: string): boolean {
    return false;
}

class Foo {
    doSomething(): void { }
}
let foo: Foo = new Foo();
foo.doSomething();

// window.onmousedown = function (mouseEvent: MouseEvent) {
//     console.log(mouseEvent.button);
//     console.log(mouseEvent.foo);
// };

